# ClaudeTower — `claudetower accounts enable` 동의 고지 문구 초안 (2026-07-12 준비 → 2026-07-15 보류 → 2026-07-27 재개)

> **[2026-07-15] 이 문구는 사용되지 않는다** — Account 모듈 자체가 보류 확정됐다
> (`.PRD/07_OAUTH_FLOW_SPEC.md §3·§4` 참고). 이 초안은 "동의 절차를 어떻게 설계하려 했는지"의
> 기록으로만 남긴다.

> **[2026-07-27] 재개** — 아래 초안을 하이브리드(OAuth+API키) 리스크 반영해 개정했다
> (`07_OAUTH_FLOW_SPEC.md §5` 참고). 이 시점엔 여전히 CLI 어디에도 연결되지 않았었다.

> **[2026-08-20 정정 — 이 문서는 이제 stale하다]**: 위 "여전히 CLI 어디에도 연결되지
> 않았다"는 서술은 더 이상 사실이 아니다. M36(2026-08-19)부터 이 초안은
> `src/accounts/consent-text.js`로 실제 코드에 반영돼 `claudetower accounts enable`
> 실행 시 라이브로 출력된다. 게다가 그 라이브 버전은 이후(2026-08-19) 실제 CLI 동작과
> 대조해보니 아직 없는 기능(`--import`, 자동전환 엔진)을 "된다"고 과대 고지하고 있던
> 오류를 스스로 발견해 이미 한 차례 정정까지 마쳤다(현재 버전:
> `CONSENT_TEXT_VERSION = 'v2.1-hybrid-scope-clarified'`). 즉 아래 "## 초안" 섹션의
> 문구는 그 정정 **이전** 버전인 채로 멈춰 있어, 지금 읽으면 실제 동의 화면과 다른
> 내용을 사실처럼 보여준다 — **실제 동의 문구의 출처는 이제 이 문서가 아니라
> `src/accounts/consent-text.js`다.** 이 문서는 "동의 절차를 어떻게 설계하려 했는지"의
> 역사적 기록으로만 남긴다(1단계 재개 결정문에 이미 있던 표현과 같은 정신).

> **이 문서의 성격(2026-07-27 작성 시점 기준, 지금은 위 정정 참고)**: M6 게이트 준수 중
> 작성된 "준비" 문서였다. 04_PROJECT_SPEC.md 130~148행의 "README·가이드 문서 요구사항"을
> CLI 실행 시점 고지문으로 옮겨 쓴 것이다.

---

## 요구사항 근거

- 04_PROJECT_SPEC.md 132행: "이 기능은 여러분의 Claude 계정이 정지될 수도 있는 위험이
  있습니다. 켜지 않아도 상태표시줄은 정상 작동합니다"를 **굵은 글씨로 명시**할 것
- 04_PROJECT_SPEC.md 117행: 여러 계정 자동 순환이 Anthropic 이용약관과 충돌할 가능성이
  있다는 것을 반드시 알릴 것
- 명시적 동의 없이는 다음 단계(OAuth 로그인)로 진행하지 않을 것(07_OAUTH_FLOW_SPEC.md 1단계)
- 대상 독자는 비개발자 — 전문용어 없이 쓸 것(04_PROJECT_SPEC.md 128행)

---

## 초안 (터미널에 그대로 출력될 문구)

```
⚠️  계정 자동 전환 기능을 켜기 전에 꼭 읽어주세요

지금까지 쓰신 상태표시줄(화면 아래 표시 기능)은 이 기능과 완전히 분리되어 있고,
계속 그대로 안전하게 작동합니다. 아래 내용은 "여러 계정을 자동으로 바꿔가며 쓰는
기능"에만 해당합니다.

1. 이 기능은 여러분의 Claude 계정 여러 개를 등록해두고, 사용량이 다 차면 자동으로
   다음 계정으로 바꿔줍니다. 로그인 계정(구독)과 API 키 계정을 섞어서 등록할 수
   있습니다. `--import` 옵션으로 이미 이 컴퓨터에 로그인된 Claude Code 계정을
   재로그인 없이 그대로 가져올 수도 있습니다.

2. **로그인 계정(구독, Free/Pro/Max)을 등록하는 경우**: Anthropic(Claude를 만든 회사)의
   이용약관에는 구독 계정을 자동으로 순환시키는 행위가 명시적으로 허용되어 있지
   않습니다. `--import`로 재로그인을 생략하고 가져온 계정도 동일하게 해당됩니다.
   사용 중 계정이 일시 정지되거나 제한될 수 있으며, 이런 일이 생기더라도
   ClaudeTower(이 프로그램)가 책임지지 않습니다.

2-1. **API 키 계정을 등록하는 경우**: Anthropic 이용약관은 API 키를 통한 접근을
   자동화 금지 조항의 명시적 예외로 인정합니다. 다만 여러 개의 API 키를 동시에
   발급받아 로테이션하는 행위 자체가 별도의 남용 방지 조항에 걸리는지는
   **확인되지 않았습니다** — 안전하다고 단정하지 않습니다.

3. 계정 정보(로그인 토큰)는 이 컴퓨터의 운영체제 금고
   (Windows: Credential Manager, macOS: Keychain, Linux: Secret Service)에만
   저장됩니다. 어디로도 전송되지 않습니다.

4. 언제든지 `claudetower accounts disable`로 다시 끌 수 있고, 꺼도 등록해둔 계정
   정보는 남아있습니다(완전히 지우려면 `claudetower account-purge`).

계속 진행하시겠습니까? 위 내용을 읽고 이해했다면 y를 입력해주세요. [y/N]:
```

---

## 검토 필요 사항 (초안 단계에서 미해결)

- [ ] 실제 정지 사례가 몇 건 있었는지, 어느 정도 확률인지에 대한 근거 자료가 없다 —
  "정지될 수도 있다"는 가능성 표현을 유지할지, 더 구체적인 수치를 넣을지는 [법무 검토 필요]
  (04_PROJECT_SPEC.md 183행과 동일한 미해결 이슈)
- [ ] `y` 외의 답변(빈 입력, 다른 문자)을 어떻게 처리할지 — 03_PHASES.md M3의 PATH 등록
  선례("애매한 답은 안전하게 미실행")를 그대로 따르는 것을 제안하나, 최종 결정은 게이트
  해제 후 실제 CLI 구현 시점에 확정
- [ ] 이 문구가 README의 "② 계정 자동전환" 섹션과 내용이 겹치는 부분을 어떻게 나눌지
  (터미널 문구는 짧게, README는 상세하게 — 원칙만 정하고 실제 README 갱신은 구현 이후)
- [ ] **(2026-07-27 신규)** 하이브리드 리스크 차등 문구(항목 2/2-1 분리) 실제 터미널 출력
  시 줄바꿈·가독성 재검토 필요(구현 시점)

---

## 차별화 프레이밍 (2026-07-27 추가)

`jung-wan-kim/teamclaude` 실측 조사 결과, 이 도구는 README·CLI 어디에도 이런 동의 게이트가
없다 — 계정을 등록하면 별도 확인 없이 바로 작동한다. ClaudeTower의 `[y/N]` 명시적 동의 절차는
teamclaude 대비 **의도적 차별화 지점**이다: 사용자 자율성을 존중하되 위험을 숨기지 않고,
"몰랐다"는 상황을 만들지 않는다. `04_PROJECT_SPEC.md`의 "AS-IS, 책임 제한" 원칙과 같은 맥락.
