'use strict';

// claudetower accounts enable 실행 시 출력할 동의 고지 상수.
// 원문은 .PRD/08_ACCOUNTS_ENABLE_CONSENT_DRAFT.md 2026-07-27 하이브리드 개정판 "초안"
// 섹션을 그대로 옮긴 것이었으나, M36(2026-08-19)부터 enable/add --api-key가 실제로
// 라이브가 된 뒤 실행으로 대조해보니 이 문구가 아직 없는 기능(--import, 자동전환 엔진)을
// "된다"고 말하고 있었다(2026-08-19 재검증에서 발견 — `accounts add --import` 실행 시
// 그 옵션 자체를 인식조차 못 함, `startProxyServer`는 bin/claudetower.js 어디서도
// 호출되지 않아 등록해도 실제 전환은 물리적으로 일어나지 않음). 동의 화면은 "위험을
// 알리는" 문서라 실제보다 더 된다고 말하면 안 된다는 원칙(04_PROJECT_SPEC.md "이용약관
// 위반 가능성 고지 없이 배포하지 마"의 반대쪽 오류 — 여기서는 "능력을 과대 고지"하지
// 않는 것도 같은 정신)에 따라, 지금 실제로 되는 것과 목표(아직 안 되는 것)를 명확히
// 구분하도록 정정했다.
//
// bin/claudetower.js에 enable/add/list/disable 서브커맨드 + account-purge 최상위 명령으로
// 전부 연결됨(M36 + 2026-08-19 fc4cefe에서 account-purge 신설·배선까지 완료, 라이브
// enable→add→list→purge 전체 흐름 실측 검증됨). 4번 항목이 약속하는 내용은 이제 전부
// 실제로 동작한다 — 이 주석이 한동안 "purge는 여전히 미구현"이라고 반대로 적혀 있었던
// 것은 이 파일을 고친 바로 그 커밋 안에서 이미 stale해진 주석이었다(2026-08-20 발견·정정).

const CONSENT_TEXT_VERSION = 'v2.1-hybrid-scope-clarified';

const CONSENT_TEXT = `⚠️  계정 자동 전환 기능을 켜기 전에 꼭 읽어주세요

지금까지 쓰신 상태표시줄(화면 아래 표시 기능)은 이 기능과 완전히 분리되어 있고,
계속 그대로 안전하게 작동합니다. 아래 내용은 "여러 계정을 자동으로 바꿔가며 쓰는
기능"에만 해당합니다.

1. 이 기능은 여러분의 Claude 계정 여러 개를 등록해두고, 사용량이 다 차면 자동으로
   다음 계정으로 바꿔주는 것을 목표로 합니다. 다만 지금 이 버전에서 실제로 할 수
   있는 것은 API 키 계정을 등록·목록 확인·끄기까지입니다 — 로그인 계정(구독) 자동
   등록, 이미 로그인된 Claude Code 계정을 가져오는 기능(--import), 사용량 감지 후
   실제 자동 전환은 아직 개발 중이며 지금은 작동하지 않습니다(계정을 등록해도 지금은
   실제로 전환되는 일이 일어나지 않습니다).

2. 앞으로 로그인 계정(구독, Free/Pro/Max) 자동 등록·전환 기능이 추가되면: Anthropic
   (Claude를 만든 회사)의 이용약관에는 구독 계정을 자동으로 순환시키는 행위가 명시적
   으로 허용되어 있지 않습니다. 사용 중 계정이 일시 정지되거나 제한될 수 있으며, 이런
   일이 생기더라도 ClaudeTower(이 프로그램)가 책임지지 않습니다.

2-1. API 키 계정을 등록하는 경우(지금 실제로 되는 기능입니다): Anthropic 이용약관은
   API 키를 통한 접근을 자동화 금지 조항의 명시적 예외로 인정합니다. 다만 여러 개의
   API 키를 동시에 발급받아 로테이션하는 행위 자체가 별도의 남용 방지 조항에 걸리는지는
   확인되지 않았습니다 — 안전하다고 단정하지 않습니다.

3. 계정 정보(현재는 API 키 값)는 이 컴퓨터의 운영체제 금고
   (Windows: Credential Manager, macOS: Keychain, Linux: Secret Service)에만
   저장됩니다. 어디로도 전송되지 않습니다.

4. 언제든지 claudetower accounts disable로 다시 끌 수 있고, 꺼도 등록해둔 계정
   정보는 남아있습니다(완전히 지우려면 claudetower account-purge — 삭제 전 다시
   한 번 확인을 거칩니다).

계속 진행하시겠습니까? 위 내용을 읽고 이해했다면 y를 입력해주세요. [y/N]:`;

module.exports = { CONSENT_TEXT, CONSENT_TEXT_VERSION };
