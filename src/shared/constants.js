'use strict';

// 프로젝트명은 가제(상표 저촉 여부 법무 검토 전, .PRD/01_PRD.md §7).
// 개명 시 이 파일 하나만 고치면 되도록 다른 코드는 전부 이 상수를 참조한다.
const CLI_NAME = 'claudetower';
const CONFIG_DIR_NAME = `.${CLI_NAME}`;

module.exports = { CLI_NAME, CONFIG_DIR_NAME };
